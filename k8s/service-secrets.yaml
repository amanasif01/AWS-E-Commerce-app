apiVersion: v1
kind: Secret
metadata:
  name: service-credentials
type: Opaque
stringData:
  # These are example credentials - in a real environment 
  # you would use actual secure credentials
  REDIS_PASSWORD: "redis-secure-password"
  PAYMENT_API_KEY: "payment-service-api-key"
